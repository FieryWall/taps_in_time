# Taps in Time

Flutter app for counting taps within a set time limit. Set a timer (MM:SS), tap as fast as you can, see your score. Supports multitouch. Keeps history of last 5 results.

## Build & Run

```bash
flutter run              # debug (with hot reload)
flutter run --release    # release (install on device)
```
